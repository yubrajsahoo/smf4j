{
  "name": "http.requests.total",
  "description": "Total incoming HTTP requests",
  "enable": true,
  "tags": [
    {
      "key": "method",
      "value": "#result"
    },
    {
      "key": "outcome",
      "value": "#error == null ? 'SUCCESS' : 'FAILURE'"
    }
  ]
}