<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.yubrajsahoo</groupId>
    <artifactId>smf4j</artifactId>
    <version>0.0.1</version>
    <packaging>pom</packaging>

    <name>smf4j</name>
    <description>Parent POM for SMF4J - Simple Metrics Facade for Java</description>
    <url>https://github.com/yubrajsahoo/smf4j</url>

    <modules>
        <module>smf4j-core</module>
    </modules>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>Yubraj Sahoo</name>
            <url>https://github.com/yubrajsahoo</url>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:git://github.com/yubrajsahoo/smf4j.git</connection>
        <developerConnection>scm:git:ssh://github.com:yubrajsahoo/smf4j.git</developerConnection>
        <url>https://github.com/yubrajsahoo/smf4j</url>
    </scm>

    <issueManagement>
        <system>GitHub Issues</system>
        <url>https://github.com/yubrajsahoo/smf4j/issues</url>
    </issueManagement>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Framework Versions -->
        <spring-boot.version>3.5.16</spring-boot.version>

        <!-- Security overrides / explicit versions -->
        <assertj.version>3.27.7</assertj.version>
        <logback.version>1.5.19</logback.version>
        <jackson.version>2.21.4</jackson.version>

        <!-- Test plugin versions -->
        <jacoco.version>0.8.13</jacoco.version>
    </properties>

    <!-- Global dependencies common to all modules (usually test deps or very common APIs) -->
    <dependencies>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!-- Dependency Management to fix versions across all modules without including them automatically -->
    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Internal Modules -->
            <dependency>
                <groupId>io.github.yubrajsahoo</groupId>
                <artifactId>smf4j-core</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Overrides / Future libraries (Yaml, etc) -->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <!-- JaCoCo Plugin for Code Coverage -->
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
